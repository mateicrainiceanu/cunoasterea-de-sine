import { Form } from "./QuizComponents/Form";
import React from "react";

function Quiz() {
	return (
		<main className="main">
			<section className="inner-page">
				<Form />
			</section>
		</main>
	);
}

export default Quiz;
